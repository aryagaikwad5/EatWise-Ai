import UploadCard from "../components/predict/UploadCard/UploadCard";

function Predict() {
    return (
        <div className="predict-page">
            <UploadCard />
        </div>
    );
}

export default Predict;